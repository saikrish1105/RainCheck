{
  "name": "raincheck-summary",
  "version": "0.1.0",
  "description": "RainCheck Summary — minimal Chrome extension that generates a rate-limit handoff summary for the current Claude.ai conversation.",
  "private": true,
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
